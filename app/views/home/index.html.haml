.container-fluid
  %h1 Letters
  .row.row-cols-1.row-cols-md-3.mt-4
    - @letter.each do |letter|
      .col.mb-4
        .card
          .card-body
            %h5.card-title= letter.title
            %p.card-text= letter.body
